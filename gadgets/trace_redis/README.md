# trace_redis

The `trace_redis` gadget traces Redis commands and replies, including errors and
command latency.

Check the full documentation on https://inspektor-gadget.io/docs/latest/gadgets/trace_redis
